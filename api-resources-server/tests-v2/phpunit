#!/bin/bash

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PROJECT_DIR="$(dirname "$SCRIPT_DIR")"

docker run --rm \
  -v "$PROJECT_DIR:/app" \
  -w /app/tests-v2 \
  reg.localbin.dev/neuedev/core/docker/php-dev:8.3 \
  vendor/bin/phpunit "$@"
